export interface BasicInfoProps {
  section: {
    businessName: string;
    operator: string;
    address: string;
    city: string;
    zipCode: string;
    email: string;
    phone: string;
  };
}

export const BasicInfoSection = () => {
  return <></>;
};
